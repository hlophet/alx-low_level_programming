#include <stdio.h>
/**
 * main: entry point
 * hello, printf task
 * return: return 0 after printing
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
