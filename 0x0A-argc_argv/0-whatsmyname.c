#include "main.h"
#include <stdio.h>
/**
 * main - prints the name of the program
 * @argc: numberof command line arguements
 * @argv: array of size argc
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);
return (0);
}
