#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * strtow - splits a string into words
 * @str: string to be split
 * Return: pointer - to an array of strings (words)
 *         or NULL - if str = NULL, str = "", etc
 */
char **strtow(char *str)
{
	return (str);
}
